export interface User {
  name: string;
  email: string;
  role: string;
  profilePictureUrl: string;
  phoneNumber: string;
}

export interface Profile {
  name: string;
  email: string;
  profilePictureUrl: string;
  phoneNumber: string;
}
